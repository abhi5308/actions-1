name: my First workflow

on: push

jobs:
    first_job:
        runs-on: Ubuntu-latest
        steps:
            - name: welcome message
              run: echo"hello"

            - name: list files
              run: ls

            - name: Read files
              run: cat README.md


